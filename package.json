{
    "name": "gerardoperruccisite",
    "version": "0.1.0",
    "scripts": {
      "postinstall": "npm install --prefix api && npm install --prefix site",
      "start": "npm start --prefix api && npm build --prefix site"
    }
  }